export interface ExperienceArticles {
    fa: string, 
    date: string,
    company: string, 
    title: string, 
    info, 
    city: string,
}

export interface SkillsArticles {
    fa: string;
    title: string;
    info: string;
}